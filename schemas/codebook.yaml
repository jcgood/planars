# NOTICE: This file has been split into focused schema modules (issue #77).
#
# The content previously in this file now lives in:
#
#   schemas/diagnostic_criteria.yaml  — diagnostic criteria (annotation columns):
#                                        names, allowed values, linguistic definitions
#   schemas/diagnostic_classes.yaml   — analysis class definitions, applicability,
#                                        required/optional criteria, qualification rules
#   schemas/planar.yaml               — planar structure ontology: structural columns,
#                                        element conventions, label standards
#   schemas/terms.yaml                — analytical vocabulary: strict/loose span,
#                                        partial/complete position, keystone, etc.;
#                                        also chart label glossary
#
# All code and documentation that previously referenced this file now points to
# schemas/diagnostic_criteria.yaml (for criterion/analysis lookups) or to the
# other files above as appropriate.
