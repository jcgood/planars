% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_counts.R
\name{plot_tree_counts}
\alias{plot_tree_counts}
\title{Tree-count bar charts}
\usage{
plot_tree_counts(
  bundle,
  chart = c("by_class", "bundles", "all", "without_adjacent")
)
}
\arguments{
\item{bundle}{A bundle from [read_planars_bundle()].}

\item{chart}{`"by_class"` (one bar per domain type), `"bundles"` (one per
class bundle), `"all"` (all tests), or `"without_adjacent"` (all tests
vs. without size-2 spans).}
}
\value{
A ggplot object with attributes `planarsviz_size`,
  `planarsviz_units` (`"in"`) and `planarsviz_transparent`.
}
\description{
How many maximal laminar families (trees) the data allows, as bar charts.
Reproduces `nyan1308_tree_count_by_class.pdf`, `_bundles.pdf` (transparent
horizontal "house style"), `_all.pdf` and `_without_adjacent.pdf`.
}
