% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_trees.R
\name{planarsviz_summary_tree}
\alias{planarsviz_summary_tree}
\title{One summary tree weighted by family frequency}
\usage{
planarsviz_summary_tree(
  bundle,
  family_id,
  member_ids,
  title,
  alpha_name,
  legend = TRUE,
  weight = c("families", "tests", "none"),
  branch_size = 4,
  emphasis_range = NULL,
  emphasis_size = 2.5
)
}
\arguments{
\item{bundle}{A bundle from [read_planars_bundle()].}

\item{family_id}{The family to draw.}

\item{member_ids}{Family ids the frequencies are counted over.}

\item{title}{Plot title.}

\item{alpha_name}{Name of the opacity scale.}

\item{legend}{Keep the opacity legend (`FALSE` hides it).}

\item{weight}{What a branch's opacity and thickness show: `"families"` (the
share of `member_ids` containing the span), `"tests"` (the span's
convergence as a share of the drawn tree's best-tested span), or
`"none"` (every branch drawn solid — for illustrating shape alone).}

\item{branch_size}{Thickness of a fully supported branch; thinner branches
scale down from it. With `weight = "none"` every branch is fully
supported, so this is simply the line width of the whole tree.}

\item{emphasis_range}{`c(left, right)` positions to trace, or `NULL`. The
edges drawn thicker are those running down to the two positions from
the smallest node containing both — the path that marks where the
stretch begins and ends.}

\item{emphasis_size}{Thickness of those edges.}
}
\value{
A ggtree plot.
}
\description{
Draws one family's tree; each branch's opacity and thickness show the
share of the comparison families that contain that span.
}
