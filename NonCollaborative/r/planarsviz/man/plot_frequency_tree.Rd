% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_trees.R
\name{plot_frequency_tree}
\alias{plot_frequency_tree}
\title{Frequency tree}
\usage{
plot_frequency_tree(
  bundle,
  selection = "consensus_all",
  title = NULL,
  weight = c("families", "tests", "none"),
  branch_size = NULL,
  emphasis = NULL,
  emphasis_size = 2.5
)
}
\arguments{
\item{bundle}{A bundle from [read_planars_bundle()].}

\item{selection}{A selection from `selections.tsv`.}

\item{title}{Plot title; the default names the number of families.}

\item{weight}{What branch opacity and thickness show: `"families"`
(default), `"tests"`, or `"none"`. See [planarsviz_summary_tree()].}

\item{branch_size}{Thickness of a fully supported branch. Defaults to 4,
the weighted charts' heaviest line; with `weight = "none"`, where every
branch would be drawn at that weight, it defaults to 0.5 — ggtree's own
default, so an unweighted tree matches the exemplary trees.}

\item{emphasis}{A `highlight_id` from `highlights.tsv` (e.g.
`"orthographic_word"`) whose first and last positions are traced with
thicker edges, or `NULL`.}

\item{emphasis_size}{Thickness of the traced edges.}
}
\value{
A ggtree plot with attributes `planarsviz_size`,
  `planarsviz_units` and `planarsviz_folder` (its `results/planarsviz`
  subfolder).
}
\description{
The most consensus-like family (by default), with each branch weighted by
the share of all maximal families containing it. Reproduces
`nyan1308_freqtree.pdf`.
}
