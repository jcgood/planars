% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conflict_groups.R
\name{plot_conflict_groups}
\alias{plot_conflict_groups}
\title{Conflict groups}
\usage{
plot_conflict_groups(
  bundle,
  other_label = "neither",
  panel_titles = TRUE,
  title_size = 24
)
}
\arguments{
\item{bundle}{A bundle from [read_planars_bundle()].}

\item{other_label}{Title text for the group with no defining span.}

\item{panel_titles}{Show each panel's title ("All 69 families", "Group A:
[5–13] (10 families)", ...). `FALSE` reproduces the old chart, whose
titles never appeared.}

\item{title_size}{Panel title size in points (the canvas is 24 x 20 in).}
}
\value{
A patchwork plot with attributes `planarsviz_size`,
  `planarsviz_units`, `planarsviz_folder` (its `results/planarsviz`
  subfolder), and `planarsviz_parts` (one list of trees per panel,
  all-families first).
}
\description{
Every maximal family stacked in one large panel, and below it one panel
per conflict group (families containing a defining span, and the rest),
each group capped at the exporter's `conflict_group_cap` trees.
Reproduces `nyan1308_conflict_groups.pdf`.
}
