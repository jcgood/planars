% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundary_strength.R
\name{plot_boundary_strength}
\alias{plot_boundary_strength}
\title{Boundary-strength bar chart}
\usage{
plot_boundary_strength(bundle, subset = NULL, bar_colour = "#7876B1")
}
\arguments{
\item{bundle}{A bundle from [read_planars_bundle()].}

\item{subset}{`NULL` for the full analysis, or a `subset_id` from `subsets.json`.}

\item{bar_colour}{Bar colour.}
}
\value{
A patchwork plot with attributes `planarsviz_size`,
  `planarsviz_units` (`"in"`), and `planarsviz_folder` (its
  `results/<dataset>/` subfolder).
}
\description{
For each position, how strongly it is a left (top panel) or right (bottom
panel) constituent edge: bars sum each span's family count over the spans
starting or ending there; black marks count the families with at least
one such span, which can't exceed the dotted line at the family count.
Reproduces `nyan1308_boundary_strength.pdf`; with `subset = "no_tono"`,
`nyan1308_boundary_strength_no_tono.pdf`.
}
